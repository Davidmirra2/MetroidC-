To use the code, you need to install an IDE and a compiler. For development, we used Visual Studio Community 2022 and MSVC.
You need to install Qt5 (or Qt6). Before using CMAKE, you need to extract the "dir" package inside the source code. Then, use CMAKE to configure the project before launching it by linking the various required libraries.
